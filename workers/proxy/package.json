{
  "name": "ddm-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker that adds Cross-Origin Resource Sharing (CORS) headers to responses from a small allow-list of public agency telemetry endpoints (NRCS AWDB, USACE Dataquery, USBR Hydromet, NWRFC). Companion to the Dynamic Drought Module (DDM).",
  "license": "CC-BY-NC-SA-4.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
