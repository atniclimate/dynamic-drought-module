{
  "name": "ddm-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker that adds Cross-Origin Resource Sharing (CORS) headers to responses from a small allow-list of public agency endpoints (see ALLOW_LIST in src/index.ts). Companion to the Dynamic Drought Module (DDM).",
  "license": "PolyForm-Strict-1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.115.0"
  }
}
