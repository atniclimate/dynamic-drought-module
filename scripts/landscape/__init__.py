"""Landscape-signature build package (build-time only; never shipped).

Extracted from scripts/build_landscape_signature.py in T-M0-1 with parity
pinned by tests/fixtures/legacy/. Entry point:
  .venv/Scripts/python.exe -m scripts.landscape --smoke --output <path>
See core.py for the pipeline seams and adapters/ for the source families.
"""
