// The plural 'simulators' must trip on its own (the singular alternation
// once missed it); this fixture holds ONLY the plural so a regression on
// the plural cannot hide behind another matching word.
export const blurb = 'compare fire spread simulators side by side';
