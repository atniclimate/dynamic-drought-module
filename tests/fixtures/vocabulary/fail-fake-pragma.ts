/** NEGATIVE fixture (T-P0-6): pragma text INSIDE a string literal is
 * rendered copy, not an allowance; this must still be found. */
export const forged = 'DDM warning // vocab-allow: this is rendered text';
