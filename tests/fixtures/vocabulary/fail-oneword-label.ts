/** NEGATIVE fixture (T-P0-6): a one-word CAPITALIZED label (a DDM button,
 * heading, or badge naming itself a Warning) must be found; the lowercase
 * key/role exemption must not cover it. */
export const badButtonLabel = 'Warning';
export const badBadge = 'Forecasts';
