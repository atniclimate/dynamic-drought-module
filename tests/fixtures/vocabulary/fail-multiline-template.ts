/** NEGATIVE fixture (T-P0-6): a banned word deep inside a multi-line
 * template (no pragma at the template's anchor) must be found. */
export const multi = `
  <div class="brief">
    A drought warning composed by the module itself.
  </div>`;
