// A single-token capitalized label from the simulate family must trip via
// the CAPITALIZED_TOKEN whole-literal rule (lowercase single tokens stay
// keys/roles/classes by design).
export const chipLabel = 'Simulator';
