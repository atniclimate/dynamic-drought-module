/**
 * Recovery impact-panel runtime entry.
 *
 * This URL is deliberately distinct from the primary dynamic entry. It is
 * used only after a primary load failure in the same document session.
 */
export * from './impact-panel-runtime';
